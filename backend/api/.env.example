# Server Configuration
PORT=3000
NODE_ENV=development

# Firebase Admin SDK
# Set GOOGLE_APPLICATION_CREDENTIALS to path of your service account key JSON file
# Or use Application Default Credentials (ADC) for local development
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
FIREBASE_PROJECT_ID=teamapi-96507

# CORS Origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
